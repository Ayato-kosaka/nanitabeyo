{
	"name": "error-triage",
	"version": "0.0.0",
	"private": true,
	"description": "本番ログ（BigQuery）エラーの定期トリアージ（#1196）。PR1 は外部I/Oを持たない純関数群のみ。",
	"scripts": {
		"test": "jest"
	},
	"devDependencies": {
		"jest": "~29.7.0"
	},
	"jest": {
		"testEnvironment": "node",
		"testMatch": [
			"<rootDir>/**/*.test.js"
		]
	}
}
