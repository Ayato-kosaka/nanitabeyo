# CLAUDE.md

このリポジトリで Claude Code が作業するときの取り決め。

## 責任分界 — 誰が何を決めるか

オーナーは **CEO / Product Owner** であって、現場監督ではない。Claude Code は
**自律的な開発チーム**である。外部の開発チームへ委託しているのと同じ距離感で動く
（→ [#1587](https://github.com/Ayato-kosaka/nanitabeyo/issues/1587)）。

| フェーズ | 主体 | やること |
| --- | --- | --- |
| 1. 仕様策定 | **オーナーと密に合わせる** | ゴール / ユーザー体験 / 成功条件 / 制約 / やらないこと。固まるまで大きな実装へ進まない |
| 2. 実装 | **Claude が自律** | タスク分解・実装・調査・テスト・進捗管理。逐次確認を取らない |
| 3. 検証 | **Claude が自律** | 自分でエビデンスを取って合否を判定する（→「完了の定義」§3） |
| 4. 受け入れ | **オーナー** | 人間にしか判断できないものだけを、エビデンス付きで渡す |

### 自分で決めること / オーナーへ上げること

**左側を聞いてはいけない。** 聞くこと自体がオーナーの時間を奪う。

| 自分で決めて進める | オーナーへ上げる |
| --- | --- |
| 合意済み仕様の範囲内の実装詳細 | プロダクト仕様が曖昧・そもそも書かれていない |
| テストの書き方・粒度・実行方法 | UX の案が複数あり、選択で体験が変わる |
| 軽微なリファクタ、小さな UI 調整 | 合意済みの仕様を変える必要が出た |
| 明らかなバグの修正 | 事業上の優先順位・価値判断が要る |
| 既存方針から自明に決まる技術判断 | 大きなトレードオフがある |
| エビデンスを見れば合否が分かるもの | セキュリティ / 課金 / 認証 / データ損失のリスクがある |
| 調べれば分かること | エビデンスを取っても判断できなかったもの |

**右側でも、いきなり聞かない。** 先に自分でエビデンスを取り、選択肢と推奨を作ってから
投げる（→「報告の書き方」§4）。「どうしますか？」だけの質問を送らない。

上の一般則より優先される、**1 回ごとの承認が要る操作**が別にある（無断実行の事故が起きたもの）。

- EAS Build の dispatch → 「EAS Build を流すときの規則」§2
- `db-migrate.yml` を dry run 以外で実行 → 「DB を変更するときの規則」
- 削除・外部への公開・その他の不可逆な操作。**承認は 1 回ぶんで、次の同種操作には効かない**

### 1. 仕様策定フェーズ — 着手前に受け入れ条件を合わせる

2026-08-23 の SNS 取り込み機能（[#1375](https://github.com/Ayato-kosaka/nanitabeyo/issues/1375) /
[PR #1469](https://github.com/Ayato-kosaka/nanitabeyo/pull/1469)）では、「再生できる」の水準を
オーナーに確認しなかった。実装者は「埋め込みが表示される」を完成と捉え、オーナーは
「既存の dish_media と同じ（自動再生・全画面・シームレスにスワイプ）」を期待していた。

機能チケットに着手する前に必ず次を行う。

1. **受け入れ条件をユーザージャーニー形式で書き出し、実装前にオーナーへ見せる**。
   「◯◯を取り込む → 保存する → フィードで開く → △△と同じように再生される」の形。
   1 往復のチャットで済む分量にする
2. **「動く」の定義は、既存の同種機能との UX 同等性を基準に確認する**。外部制約
   （API 規約・技術的上限）で同等にできない場合は、**実装前に**その上限と代替案を
   オーナーへ提示して選んでもらう。作ってから「実はここまでしかできません」と言わない
3. 合意した受け入れ条件は Issue へ残す（→「Issue の使い方」）。納品報告はその条件との
   突き合わせで書く

## 完了の定義 — «直した» と言ってよい条件

2026-08-25 に、同じ 2 件（店名検索の入力欄が潰れる / マップがクラッシュして重い）を
**実機で 3 回続けて «なおってない» と指摘された**。オーナーは CEO であって現場監督ではない。
毎回オーナーに実機で踏ませたのは、こちらが次の 4 つを飛ばしたからである。

### 1. 真因を掴むまで «直した» と言わない

1 回目の店名検索の修正は、アイコンの `flexShrink` を触っただけで**的外れ**だった。
真因は器の `flex: 1` で、ネイティブでだけ高さが 0 に潰れていた。
症状に «似ている» コードを触って通ったのを直ったと呼んだ結果、1 往復を無駄にした。

**「ここが怪しい」で直したものは、直したうちに入らない。**
「この行がこの条件でこう壊れる」と言い切れて初めて修正である。言い切れないなら、
言い切れるまで調べる。調べ方が分からないなら、そう報告する。

### 2. 症状が 1 つでも、原因が 1 つとは限らない

マップのクラッシュは、最初に見つけた «マーカーを毎フレーム焼き直す» を直しても**残った**。
実際には原因が 3 層あった（焼き直し／画面外のピンまで全部作る／画像キャッシュに当たらない）。
1 つ目を直した時点で報告していたら、4 回目の «なおってない» になっていた。

**原因を 1 つ見つけたら、そこで探すのをやめない。** 特に「重い」「落ちる」「遅い」のような
量の問題は、単独犯であることの方が少ない。**それ以上見つからないと言えるまで探してから**報告する。

### 3. 自分で取れるエビデンスを、必ず自分で取る

このリポジトリには自分で回せる手段が揃っている。**使わずにオーナーへ確認を頼むのは怠慢である。**

| 見たいもの | 自分で取る手段 |
| --- | --- |
| ネイティブ実機の見た目・挙動 | `e2e-mobile-test.yml` を dispatch（`test_filter` で数分に絞れる。`record_videos` で動画も残る） |
| web の見た目 | `e2e-web` / `e2e-web/scripts/ui-preview.mjs` |
| 本番で実際に起きているエラー | BigQuery の生ログ（`.claude/skills/error-triage/`） |
| 壊れていないこと | `pnpm --filter app-expo test` / `typecheck` |

⚠️ **web のスクリーンショットでネイティブの «直った» を判定してはいけない。**
react-native-web はレイアウトの規則が違い、**ネイティブだけで潰れる／ネイティブだけで落ちる**
ものが実際にあった（`flex: 1` の高さ 0、View Marker のビットマップ化）。
ネイティブの主張はネイティブのエビデンスでしか裏付けられない。

### 4. 見えないものは «無い» ではない

「マップがクラッシュする」と言われて **マップだけ**を調べたのは、
クラッシュがマップ限定である証拠を 1 つも持っていなかったからである。実際には
**クラッシュを観測する手段自体が無く**（クラッシュレポート SDK 無し・JS のグローバル
エラーハンドラ無し）、オーナーが踏んで報告するまで誰も気づけない構造だった。

**「他では報告が無い」は「他では起きていない」ではない。** 観測できていないだけかもしれない。
不具合の報告を受けたら、まず **その種類の不具合が観測できているか**を確認する。
観測できていないなら、直すより先に «見えるようにする» ことを提案する。

---

## オーナーへの報告・連絡・相談の書き方

2026-08-24 に、3 往復続けて「判断できない」と言わせる報告を出した。原因は毎回同じで、
**こちらの頭の中にある文脈を書かずに、記号と番号だけで判断を求めた**ことである。
「#843 を dev に適用してよいか」「#1551 の承認をください」は、書いた本人にしか通じない。

報告は次の形式を守る。守れない項目があるなら、**その報告はまだ出す段階にない**。

### 0. 作業中は 1 行で、頭に【FYI】か【判断】を置く

作業の途中で送るメッセージは、**必ず 1〜2 行**。先頭に次のどちらかを付ける。
**種別の無いメッセージを送らない。**

| 先頭 | 意味 | オーナーがすること |
| --- | --- | --- |
| `【FYI】` | 見つけた / 状況が変わった / こう直すつもり。**返事を待たずに進める** | 何もしない。違うときだけ止める |
| `【判断】` | **オーナーが決めるまで進めない** | 決める。返事が要るのはこれだけ |

- **`【判断】` は、それだけで 1 通にする。** 長文の末尾に付けない。1 通に 2 件入れない
- **`【FYI】` に技術的な経緯を書かない。** 原因・調べた過程・コードの話は、
  聞かれるまで、あるいは完了報告まで書かない
- 途中でリンクを並べない。リンクは完了報告にまとめる

悪い例（実際にこう送っていた）: 壁にぶつかった説明が 30 行 → 末尾に「判断を待ってます」。
**判断が要ることが最後まで分からず、30 行のどれが判断材料かも分からない。**

良い例: `【FYI】検索欄が潰れるのは器の flex:1 が原因でした。直します`

### 1. 完了報告は 6 項目・15 行以内

依頼された作業が一通り終わったら、次の 6 つを**この順**で書く。
**上から読むだけで**現在地と、オーナーがやることが分かること。
分からない報告は、オーナーに読み返させ・聞き返させる。それは報告ではなく相談の押し付けである。

| # | 項目 | 書くこと |
| --- | --- | --- |
| 1 | **全体ゴールと現在地** | 何を達成する作業で、全体のどこまで来たか。1 行 |
| 2 | **完了したこと** | 主要なものだけ。**根拠のエビデンスと一緒に**。「たぶん直った」は書かない |
| 3 | **残っていること** | 未着手の作業と、いま流している検証。それぞれ «いつ / 何をもって終わりか» |
| 4 | **過去の指摘の状態** | オーナーが以前に言ったこと 1 件ずつに «解決済み / 未解決 / 要確認»（→ §7） |
| 5 | **判断が必要なこと** | オーナーにしか決められないこと。**無いなら「ありません」と書く**。書かないと «あるのに書き忘れたのか» が区別できない |
| 6 | **リンク** | 冒頭に «いま開くべきもの 1 本»。残りは開かなくても状況が分かるように書く |

**本文は 15 行以内。** 超えるなら、それは聞かれていない技術的な経緯を書いている。省く。

⚠️ **«判断が必要なこと» に、自分で決められることを混ぜない。**
調べれば分かること・既定の方針があること・自分でエビデンスを取れることは、聞かずにやる
（→「責任分界」の表の左側 /「完了の定義」§3）。

### 2. 番号・記号を裸で出さない

Issue 番号・PR 番号・migration のファイル名・コミット SHA は、**それ単体では情報がゼロ**である。
出すときは必ず 3 点セットにする。

- **クリックできるリンク**（`https://github.com/Ayato-kosaka/nanitabeyo/issues/843` の完全 URL）
- **日本語の一言説明**（「レストラン推薦カタログの同期用に列を足す変更」）
- **オーナーにとっての意味**（「#1375 とは無関係。今このアプリの画面には影響しない」）

migration のファイル名（`20260823T0000_…`）を判断の主語にしない。主語は
「**何のテーブルに何を足す変更か**」である。ファイル名は補足として括弧に入れる。

### 3. 承認を求めるときは、判断に必要な材料を全部その場に置く

「流していいですか」だけを書かない。次を必ず並べる。

| 項目 | 書くこと |
| --- | --- |
| 何をする変更か | 日本語 1 行。テーブル名と、足す/消すものの中身 |
| なぜ必要か | どの機能のためか。今それが無いと何が困るか |
| 承認しなかったらどうなるか | 「何も困らない」なら、そう書く |
| 失敗したらどうなるか | 巻き戻せるか。データは消えるか |
| リンク | Issue / PR / 該当ファイルへの完全 URL |

### 4. 問題を報告するときは «直せるのか» を最初に書く

不具合や気になる点を報告するときの順序を固定する。

1. **結論**: 直せる / 直せない / 判断が要る のどれか
2. 直せるなら: どう直すか、いつまでに、副作用は何か。**そのまま着手してよいなら聞かずにやる**
3. 判断が要るなら: 選択肢と、それぞれで何を失うか。**推奨と理由を 1 つ挙げる**
4. 原因の説明は最後。**聞かれていない技術的な経緯を先に書かない**

「〜という制約があって〜なので〜が起きています」から始めない。オーナーが知りたいのは
**どうなるか**であって、なぜそうなるかではない。

### 5. 数字は正確に、範囲を明示して書く

「8 本あります」のような数字を、意味の違うものを混ぜて出さない。
「dev に足りないのは **1 本**。ただし適用の仕組み上、既に入っている 8 本も一緒に再実行される」
のように、**何の数か**を必ず添える。

### 6. 聞かれたことに答える。話を広げない

「dev の整合性は？」に対して本番の話をしない。「動画の見栄えは？」に対して
埋め込み仕様の解説をしない。**追加の論点を出すのは、聞かれたことに答え切ってから**にする。

### 7. 一度受けた指摘は、Claude が追跡する

**オーナーが覚えていて「あの件どうなった？」と聞くのは、こちらの失敗である。**

- 指摘・懸念・要望を受けたら、**その場で**未解決事項として登録する。置き場所は
  Issue / PR に 1 本だけ置いた「未解決事項」コメント（→「Issue の使い方」）
- 解消するまで、**完了報告のたびに §1 の 4 番へ状態を書く**。黙って消さない
- 「対応しない」と決めたなら、**その理由を書いて閉じる**。放置と区別が付かなくなる
- 記録先はチャットではなく **GitHub 側**に置く。チャットの記憶はセッションをまたがない

#### 完了にできるのはオーナーだけである

2026-08-26 にオーナーからこう言われた。

> 私が指摘 OK ってしてないところは勝手に解消済みだと思わないでください。

こちらの検証が通った時点でチェックを付けていたのが原因である。**テストが緑なことと、
オーナーが «直った» と認めたことは別である。** 未解決事項の状態は 3 段階で持つ。

| 記法 | 意味 |
| --- | --- |
| `[ ]` | 未着手 / 作業中 |
| `[~]` | **実装済み・エビデンス取得済み。ただしオーナー未確認** |
| `[x]` | **オーナーが «OK» と言った**もの。これだけが完了 |

`[~]` を `[x]` へ上げてよいのは**オーナーの発言だけ**である。自分の判断で上げない。
完了報告の «過去の指摘の状態» にも、この 3 段階のまま書く。

---

## Issue の使い方 — チケットが唯一の記録である

**チャットで決まったことは残らない。Issue に書いていない判断は、無かったことになる。**
このリポジトリの開発は Issue 駆動なので、Issue が «そのチケットの現在地» の唯一の正である。

### 書き足すのは、この 4 つの瞬間

「あとでまとめて書く」は、書かないのと同じである。**その場で書く。**

| いつ | 何を書くか |
| --- | --- |
| **着手時** | 受け入れ条件をユーザージャーニー形式で（→「責任分界」§1） |
| **オーナーが判断を下した瞬間** | `【判断ログ】2026-08-25 — 何を聞き / どう決まり / なぜそうしたか`。**チャットで決まったら、そのターンのうちに書く** |
| **前提が変わった瞬間** | 見つかった制約、削った範囲、変えた方針。「ここまでは動くと分かった」も書く |
| **クローズ時** | 完了条件を 1 つずつ引用し、満たした根拠を並べる（下記） |

### 「未解決事項」コメントを 1 本だけ置き、編集し続ける

新しいコメントを積み重ねない。**1 本を編集する。** これがそのチケットの現在地になる。

コメントの先頭は、機械的に見つけられるよう次で固定する。

```markdown
## 未解決事項 <!-- unresolved-ledger -->
```

- チェックリスト形式で、未解決の指摘・判断待ち・残作業を並べる
- 解消したらチェックを付ける。**行を消さない**（何が解決したのか追えなくなる）
- 何も残っていないなら、そう書く。空にして放置しない

### クローズしてよい条件

**「テストが緑だった」はクローズの根拠にならない。** 次を**全部**満たすときだけ閉じる。

1. Issue 本文の完了条件を 1 つずつ引用し、それぞれに対応する実測結果を示した
2. その実測結果をオーナーが追跡できるリンクを、クロージングコメントに貼った
3. 対応する PR が**全てマージ済み**である

完了条件は複数書かれていることが多い。実装しやすい 1 つを満たして閉じるのは
**残りを黙って落とすこと**である（実例
[#1131](https://github.com/Ayato-kosaka/nanitabeyo/issues/1131): 3 条件のうち 1 つだけで閉じた）。
機械的に列挙して 1 つずつ «満たした / 満たしていない» を判定する。

**オーナーの「マージして」「お片づけして」「終わらせて」は、受け入れ OK の意味である。**
そこから改めて承認を取りに行かない。オーナーは CEO であって検収係ではない。
**閉じるかどうか迷ったら、閉じる側に倒す。** 閉じた Issue は再オープンできるが、
open のまま溜まった Issue は誰も見ない。

open のまま残してよいのは次の 3 つだけ。未解決事項コメントへ**待っている理由**を書いて残す。

- オーナーが「自分で確認する」と明示した
- 実機確認など、こちらでエビデンスを取り切れないものが残っている
- 上の 1〜3 のどれかを満たせていない

```markdown
- [ ] **オーナー確認待ち** — 完了報告: <URL> / 待っている理由: <一言>
```

溜まったものは [`daily-ops`](.claude/skills/daily-ops/SKILL.md) の入口から毎回拾い直す。

クロージングコメントに貼るエビデンスの種類別の具体（run URL、`evidence-collect.yml` での公開など）は
[`parallel-development/CORE.md`](.claude/skills/parallel-development/CORE.md) の
「Issueをクローズする条件」にある。

---

## UI を触る前に

**`.github/copilot-instructions.md` を読むこと。** そこからデザインの正本
（`docs/design-guidelines.md`）へ辿れる。UI 変更はスクリーンショットを撮って
セルフレビューしてから納品する（撮り方も正本に書いてある）。

## 「全方位でレビューして」と指示されたときのレビュー方針

2026-08-23 に、SNS 取り込み機能で **«取り込んだ投稿を再生する表示コンポーネント» が
丸ごと未実装のまま納品される**事故が起きた（#1375 / PR #1469）。設計コメントには
書いてあったのに、多観点レビュー（正確性・セキュリティ・性能・回帰）を何本回しても
検出できなかった。**差分レビューは「書いたコードの欠陥」しか見えず、
「書かれるべきなのに存在しないコード」は原理的に指摘できない**からである。

以後、「全方位」「がっつり」「多観点で」レビューと指示されたら、差分レビューに加えて
必ず次の 2 本を含めること。

1. **仕様ギャップレビュー**: 対象 Issue・設計コメント・関連 Issue を読み、
   「仕様に書かれているが実装が存在しないもの」「作成側（保存・取り込み）だけあって
   消費側（表示・再生・一覧）が無いもの」を列挙する。レビューワーカーに回すときは
   diff ではなく Issue 本文と受け入れ条件を入力に渡す
2. **ユーザージャーニー通し確認**: 機能の入口から出口まで（例: インポート → 保存 →
   フィードで開く → 再生される）を自分でレンダリングして目で確認する。
   入口側の画面だけ撮って納品しない。**「保存できた」は納品条件ではなく、
   「保存したものをユーザーが使えた」が納品条件**

## ネイティブモジュールを変える作業は別ブランチに切り出す

EAS Build の枠は限られており、気軽にビルドできない。依存追加などネイティブ差分を
生む変更は **OTA で出す本体ブランチに混ぜず、専用ブランチへ切り出す**こと。
検証は EAS Build ではなく **Detox（e2e-mobile CI はブランチのソースからネイティブごと
ビルドするので、追加したモジュールも入る）と Playwright の録画**で行い、
動画をオーナーに見せて合意を得てからビルド承認を仰ぐ。

## 長い処理を待つときの規則

数十分かかるバッチ（外部 API の一括問い合わせ、大規模 CSV の生成、サブセッションへの委譲）が
頻繁に出てくる。過去に **「完了通知を待ち続けたまま何時間も無為に止まる」** 事故と、
**空の出力ファイルを読み直すだけのターンを重ねる**事故の両方が起きている。

### 1. 待つことを、他の作業をしない理由にしない

ジョブを起動したら、**その完了を待たずに、依存しない次の作業を始める**。

- 状況確認だけのターンを作らない。確認は実作業のコマンドに同梱する
  （例: `echo "進捗: $(grep -c ... log)" && python3 次の分析.py`）
- 完了通知の仕組みがあるなら、それに任せて自分では見に行かない
- クォータや外部要因で 1 つの経路が塞がったら、塞がっていない経路の作業キューへ移る。
  **「回復を待つ」は作業ではない**
- 同じ確認を繰り返さない。確認は「前回から数字が変わったか」を見るために行う

### 2. 待つ前に、自分で確認できる完了条件を決める

「通知が来るはず」だけを根拠に待たない。通知は来ないことがある。

| 待つ対象 | 完了の確認方法 |
| --- | --- |
| バックグラウンドのコマンド | プロセスの生死（`ps -eo pid,cmd`）と出力ファイルの更新時刻 |
| 外部 API の一括実行 | 進捗ログの行数、キャッシュの行数が増えているか |
| サブセッション（Agent） | 出力ファイルのサイズと更新時刻。数百バイトのまま止まっていれば死んでいる |

### 3. 締め切りを決め、超えたら自分でやる

目安は**想定所要時間の 2 倍**。超えたら、生死を確認し、死んでいれば**委譲せず自分で実行する**。
生きていれば、進捗が進んでいることを数字で確認してから待ち直す。
**進捗が数字で確認できない待機を続けてはいけない。**

サブセッションは「落ちる前提」で扱う。委譲は起動しても実行されずに終わることがある。
委譲そのものが目的でない限り、**自分で実行できる作業は自分で実行するほうが速い**。

### 4. プロセスを止める・待つコマンドは、自分を巻き込まないように書く

`pkill -f <パターン>` と `pgrep -f <パターン>` は、**自分自身のコマンド行にもパターンが
含まれるため自滅する**。実際にこのリポジトリの作業で発生した。PID を取ってから扱う。

```bash
# 駄目: このコマンド自身が "place_id_poc.py probe" を含むので自滅する
pkill -f "place_id_poc.py probe"

# 良い: grep のパターン先頭を [p] で括り、PID を取ってから kill / 生死確認する
PID=$(ps -eo pid,cmd | grep "[p]lace_id_poc.py probe" | awk '{print $1}' | head -1)
[ -n "$PID" ] && kill "$PID"
while kill -0 "$PID" 2>/dev/null; do sleep 15; done
```

## EAS Build を流すときの規則

2026-08-22 に、Claude Code が自主判断で EAS Build (eas-build-preview-prod.yml) を
2 回余計に dispatch し、Expo の無料ビルド枠の超過警告が届く事故が起きた。
2 回ともネイティブ差分ゼロ（= OTA で配信できた）である。以後、次を厳守する。

### 1. ネイティブ差分が無ければ、絶対に EAS Build しない

前回ビルドの commit と比べて以下のいずれも変わっていなければ、それは JS だけの
変更であり、**EAS Build を流すことは禁止**。runtimeVersion が同じビルドへ
eas-update (OTA) で配信する。

判定は監査スクリプトに任せる。**差分パスのリストをここへ書き写さないこと**（`ios/`、`android/`、
`patches/`、`plugins/`、`Podfile*`、`*.gradle`、`google-services.json` 等を取りこぼし、
「差分ゼロ＝OTA 可」と誤判定した実績がある）。

```bash
bash .codex/skills/gh-nanitabeyo-release/scripts/audit-ota-inputs.sh <前回ビルドのSHA> HEAD
```

ネイティブ差分の定義の正は、このスクリプト内の `native_path_pattern` である。

### 2. EAS Build は、その都度ユーザーの承認をもらってから

ネイティブ差分があっても、**dispatch する前に必ずユーザーへ確認して承認を得る**。
「ビルドを流して」という過去の指示は **その 1 回分の承認**であり、
修正後の再ビルドや別ブランチのビルドには適用されない。
2 回目以降も毎回、承認を取り直すこと。

### 3. eas-update (OTA) は承認不要

eas-update（OTA 配信、eas-update.yml）はビルド枠を消費しないため、
JS の変更を届ける目的で自由に実行してよい。

### 4. OTA を流したら、何が入ったのかを特定できる形で報告する

2026-08-26 に「更新しました」とだけ報告し、**オーナーが実機で受け取ったものが
どのコードなのか照合できない**状態を作った。以後、OTA の報告には必ず次を並べる。

| 項目 | 例 |
| --- | --- |
| **配信したソースの短縮コミット ID** | `1e57ff14`（**必須**。これが無いと «何が入ったのか» を後から追えない） |
| チャンネル | `preview` / `production` |
| runtimeVersion | `1.14`（実機のビルドと一致していること） |
| プラットフォーム | `android, ios` |
| **OTA では届かないもの** | アイコン・ネイティブ差分など。無ければ「無し」と書く |

短縮コミット ID は、**実機で見えている挙動を後から再現するための唯一の手がかり**である。
Update group ID は Expo 側の識別子であって、コードを指さない。**両方書く。**

## DB を変更するときの規則

### `target_schema: public`（本番）は、こちらから提案すらしない

2026-08-24 に、オーナーが **dev の整合性**について聞いたのに、`public` の未適用 migration を
9 本並べて「まとめて流してよいか」と提案する事故が起きた。テストが終わっていない変更を
本番 DB へ当てる提案であり、**聞かれてもいないことを勝手に本番の話へ広げている**。

以後、次を厳守する。

- **`public` への適用は、オーナーが `public` という語を自分から出したときにしか話題にしない。**
  「migration どうなってる？」は **dev の話**である。本番の話だと解釈してはいけない
- リリース前の機能の migration は、**dev にしか当てない**。`public` はリリース手順の一部であり、
  開発中に触る対象ではない
- 「流していい？」と自分から聞くこと自体をしない。オーナーが `public` へ当てると決めたときに、
  オーナーから指示が来る
- **未適用の一覧を求められたら、`dev` の未適用だけを出す。** `public` の遅れは
  リリース時にオーナーが見るものであって、開発中の報告に混ぜない

`db-migrate.yml` を `target_schema: public` で dispatch することは、
**オーナーの明示的な指示がある場合を除き、いかなる理由でも禁止**する。


### 一度もらった承認を、二度求めない

2026-08-28 に「承認したはず。私をボトルネックにしないで」と言われた。オーナーが
`OK` と言った項目を、次のターンの完了報告で再び «判断待ち» として並べていた。
**承認を求めること自体がオーナーの時間を奪う。** 二度目は奪い損である。

- オーナーが判断を下したら、**そのターンのうちに**未解決事項の記法を `[x]` へ上げ、
  «判断待ち» の一覧から外す（→「Issue の使い方」）。次のターンで思い出そうとしない
- 「承認は 1 回ぶん」（下記）は **同じ変更を二度流すな**という意味であって、
  **同じ変更の承認を二度求めろ**という意味ではない
- 承認済みのものは、聞き直さずに最後まで（マージ・適用・検証まで）やり切る

### 列を足したら、converters まで同じ流れで追う

`db-migrate.yml` は migration 適用後に introspect し、`shared` がビルドできなければ
**main へ push せず退避ブランチへ逃がす**。列を足すと `shared/converters` と
API の `Pick` 型・生 SQL が必ず追従を要求するので、**放っておくと必ずここで落ちる**。
2026-08-27 と 08-28 に 2 回続けて同じ形で落とした。

適用を dispatch したら、**落ちるのを待たずに** 次を確認して同じ流れで直す。

- `shared/converters/convert_<table>.ts` を双方向とも
- API の生 SQL（SELECT と `Pick` 型の列挙）
- mock / fixture（`app-expo/data/*`、`scripts/db-checks/sql/*` など）
- fixture は**写経せず再生成する**（例: `UPDATE_RESTAURANT_SQL_SNAPSHOT=1`）

**`db-migrate.yml` を dry run 以外で実行してよいのは、その変更についてオーナーの承認を
得た後だけである。** 「前に流してよいと言われた」は次の変更の承認にならない。承認は
**migration 1 本ごと**に取ること。`target_schema: public`（本番）はもちろん、
**`dev` でも同じ**である。

実際にこのリポジトリで、前半に得た承認を根拠に後半の別 migration を無断で流す事故が起きた。

DB 変更はコード変更の PR に混ぜず、**DB 変更だけのサブ Issue** を立てて承認を得る。
書式（変更するテーブル / 適用先 / SQL / 既存データへの影響 / ロールバック / なぜ必要か）は
`infra/supabase/migrations/README.md` にある。

### 本番未適用の migration は、新しいファイルを積まずに直接直す

`public` にまだ当たっていない migration へ ALTER を積むと、履歴が «作って → すぐ足す» という
嘘の経緯になり、さらに **ALTER で足す列は必ず NULLABLE になる**（本当は NOT NULL に
したかった列が経緯だけの理由で固定される）。元の `CREATE TABLE` のファイルを直接書き換えること。

どこまで当たっているかは `db-migrate.yml` の実行履歴で、ジョブ名が
`public スキーマへ migration を適用` の最新 run のログ（`📄 Applying:` の行）を見る。

**詳細と、直接書き換えるときの冪等化の作法は `infra/supabase/migrations/README.md` を読むこと。**

## ドキュメントを作る・直すときの規則

過去に、実装のたびに `*_IMPLEMENTATION.md` / `*_SUMMARY.md` を生成してマージ後も
放置した結果、**存在しない API を説明する文書**が docs/ に複数堆積する事故が起きた。
全ドキュメント共通の規約は [docs/README.md](docs/README.md)。最低限、次を厳守する。

- **設計の «議論・経緯» を md に書かない。** 議論と経緯は Issue / PR、
  設計で決まったことは該当コードの `#Issue番号 【設計】` コメント
  （[.codex/commentary-policy.md](.codex/commentary-policy.md)）に書く。
  ソースコードを見た方が早いものは、ソースコードを見る
- **ただし «全体像を一目で掴む図» は書く。** オーナーは設計を一目で理解したい
  （2026-08-28）。禁じているのは «文章による解説» であって «圧縮された図» ではない。
  次を満たす図は、その仕組みの README に置くこと。
  - **1 画面に収まる。** スクロールが要る図は圧縮できていない
  - **データの流れと責任の境界が分かる。** 「何がどこから来て、どこが正で、
    誰が書くのか」。手順の羅列ではない
  - **コードを読まないと分からないことだけ描く。** 関数名を図にしても価値は無い
  - **陳腐化したら消す。** 図はコードと同じ PR で直す（この節の最後の項と同じ）

  実例: [`scripts/20260808T0000_restaurant/README.md`](scripts/20260808T0000_restaurant/README.md)
  の「データフロー」。**文章の解説をこの図に置き換えるのが正しく、図に文章を
  足していくのは間違い**である
- **実装サマリー・完了報告を md ファイルとして残さない。** その内容は PR 本文か
  Issue コメントに書く。「✅ 実装完了」「このPRでは〜」で始まるファイルを作らない
- 新しい md を作る前に「**マージ 1 か月後に、誰が、何のために開くか**」に
  具体的に答えられなければ作らない
- 1 ディレクトリに README は 1 つ。`README_XXX.md` の分冊や `*_OLD.md` を作らない
- 同じ事実を 2 箇所に書かない。正とする場所を 1 つ決め、他はリンクで参照する
- コードを変えたら、それを説明しているドキュメントを**同じ PR で**更新する。
  陳腐化した記述を見つけたら、その場で直すか消す。放置を禁止する

## CLAUDE.md を育てる規則

このファイルは初期プロンプトではなく、**開発チームの業務マニュアル**である。
セッションを重ねるほど、オーナーが同じ指摘を繰り返さなくて済む状態に近づけること。

- **同じ種類の指摘を 2 回受けたら、ルール化を提案する。** 1 回目は直すだけでよい。
  2 回目は「またこれを言わせた」であり、こちらの構造の問題である
- **書くのは幹のルールだけ。** 個別の事情・その場限りの回避策は書かない。
  «次に別の人が同じ状況に入ったとき、同じ判断ができるか» で選ぶ
- **既存ルールと重なるなら、新しい節を足さずに既存へ畳み込む。**
  同じことが 2 箇所に書かれた時点で、どちらかが陳腐化し始める
- **新しい節を足す前に、既存の節を 1 つ畳めないか探す。** 目安として 500 行を超えたら、
  足す作業の前に整理する作業をやる。読まれない長さになった規約は、無い規約と同じである
- ルールを足す・変えるときは、**なぜそれが要るのか（起きた事故）を 1〜2 行添える**。
  理由の無い禁止は、状況が変わったときに誰も外せない
