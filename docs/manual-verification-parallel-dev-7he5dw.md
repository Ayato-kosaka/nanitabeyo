# 実機確認チェックリスト（統合ブランチ `claude/parallel-dev-orchestration-7he5dw`）

このドキュメントは、統合ブランチ `claude/parallel-dev-orchestration-7he5dw` へマージした 13 件の変更のうち、
**自動テストでは守れず、人間が実機・実ブラウザで触らないと確認できない項目**をまとめたものです。

## なぜこれが要るのか

この統合ブランチの検証は、すべて次の範囲で行われています。

- jest（ユニット・コンポーネントテスト）
- `tsc --noEmit`
- Playwright（`e2e-web`。ログアウト E2E のみ）

つまり **Android / iOS の実機挙動、実ブラウザでの OAuth 往復、見た目の良し悪しは一切検証されていません。**
特に今回は SafeArea・タッチジェスチャ・OAuth リダイレクトという「エミュレータや jest では原理的に再現できない」
領域の修正が含まれます。ここを人間が確認しないと、直ったつもりで直っていない状態のままマージされます。

各項目には**何を見れば良いか**と、**なぜ自動テストで守れないのか**を書いてあります。

---

## 1. 優先度：高（直った確証がまだ無いもの）

### #1135 Web で OAuth ログインが 1 回目で入れず、2 回目で入る

**なぜ自動テストで守れないか**: 実 Google IdP との往復は bot 検知・利用規約の制約で自動化できません。
競合の解消自体はユニットテストで固定済み（修正前のコードで実際に赤くなることを 2 回の独立レビューが実測）ですが、
**「1 回目のログインで入れる」ことの最終確認だけは実ブラウザが要ります。**

- [ ] **Web で、1 回目の Google ログインでログイン状態になる**（これが完了条件そのもの）
- [ ] Android で OAuth ログインが回帰していない
- [ ] iOS で OAuth ログインが回帰していない
- [ ] ログアウト → 再ログインも 1 回目で入る

**devtools での切り分け手順**（再現した場合に原因を絞るため）:

1. Application → Local Storage の `sb-<project-ref>-auth-token` を開き、`user.is_anonymous` を控える（JWT 本体は貼らないこと）
2. Console / Network の **Preserve log** を ON にしてログイン
3. コールバック着地の直後、画面遷移する前に確認する

| 見るもの | 期待 | 外れたときの意味 |
| --- | --- | --- |
| `sb-<ref>-auth-token-code-verifier`（飛ぶ直前） | 存在する | 無ければセッション削除で verifier ごと消えている |
| 同上（着地の数秒後） | 消えている | 交換が実行された証拠 |
| `sb-<ref>-auth-token` の `user.is_anonymous`（着地後） | `false` | `false` なのに画面がゲスト表示なら React state だけ巻き戻っている |
| Network の `POST /auth/v1/token?grant_type=pkce` | 200 | 400 なら `code_verifier` 側の問題 |
| Network の `GET /auth/v1/user` | **出ないはず** | 出ていたら削除したはずの `setSession` が残っている |
| Network の `POST /auth/v1/signup` | **出ないはず** | 出ていたら匿名サインインが OAuth セッションを潰している |

**BigQuery（後追い）**: `frontend_event_logs` で 1 回目に `oauth_callback_success` が出た後に
`sessionRestored`（`payload.user_id` が匿名 ID）が続いていないこと。続いていたら未解決です。
新イベント `sessionRestoreSuperseded` / `anonymousSignInSuperseded` / `anonymousSignInDiscarded` の解釈は
`.codex/bigquery/event-catalog.md` を参照してください。**`sessionRestoreSuperseded` は正常起動でも発火する**ため、
`stale_user_id` と `current_user_id` の一致/不一致で正常系と競合を区別します。

### #1126 保存したお店リストの横スクロールで縦に広がる（Android）

**なぜ自動テストで守れないか**: 実タッチのジェスチャ競合は Android 実機でしか再現できません。
しきい値の判定ロジックはユニットテストで固定していますが、**触り心地は数値からは判断できません。**

設定値: カルーセル活性化 `activeOffsetX = ±10dp` / 縦とみなして fail `failOffsetY = ±20dp`。
理屈上、シートへ譲るのは約 63° 以上の急角度のみです。

- [ ] **レビュータブ（ログイン後）で保存店リストを普通に横スクロールしても、縦並び UI に変わらない**
- [ ] **意図的に縦へ引けば従来どおり展開できる**（機能が死んでいないこと）
- [ ] 速い横スワイプでも縦に反応しない
  - 独立レビューの指摘: `draggable=false` の反映は JS の setState を経由するため、
    **非常に速いスワイプでは prop 反映前にシートが掴む短い競合窓が理論上残ります。**実用上問題になるかは実機判断
- [ ] 斜め方向のスワイプが不自然に感じない
- [ ] iOS で横スクロール・縦展開ともに回帰していない

### #1130 お知らせ一覧の SafeArea 食い込み（Android）

**なぜ自動テストで守れないか**: SafeArea inset の実効果は実機でしか見えません。
テストは「`react-native-safe-area-context` 製の SafeAreaView に `edges={["top"]}` が渡ること」という
**構造**を固定しているだけで、見た目は保証しません。

- [ ] **Android でお知らせ一覧の上端がステータスバーへ食い込んでいない**
- [ ] **ブロック済み料理一覧と同じような上部スペース感になっている**（Issue の受入条件）
- [ ] iOS で上端の余白が増えていない
- [ ] **レビュー投稿時の料理カテゴリ検索モーダル**で、× ボタンは SafeArea にかかったまま、中身は食い込んでいない
- [ ] 同モーダルが「保存料理カテゴリの地点検索」と同じレイアウトに見える

---

## 2. 優先度：中（見た目・体感の確認）

### #1120 ログインユーザーの友達投票で一瞬ゲスト用の絵文字選びが出る

**なぜ自動テストで守れないか**: 「一瞬出る」は描画タイミングの問題で、jest では
「未確定の間はゲスト用 UI を描画しない」という不変条件までしか固定できません。

- [ ] **ログインユーザーで友達投票を行い、絵文字選びが一瞬も出ない**
- [ ] ゲストユーザーでは従来どおり絵文字選びが出る
- [ ] 確定までのローディング表示でレイアウトが跳ねない（`minHeight: 220` で確保しているが体感を確認）
- [ ] モーダルを開くたびにスピナーを経由する体感が許容範囲か

### #1122 友達投票でモーダルを開いてから「店を見る」でバグる

- [ ] **web でモーダルが閉じてから DishMediaMap へ遷移し、すぐ操作できる**
- [ ] **Android で同上**
- [ ] **iOS で回帰していない**（元々無事だったので、遷移が遅くなる・二重発火しないこと）
- [ ] 未検索の候補で「店を見る」を押し、検索中にモーダルを閉じた場合、その後に別候補のモーダルを開閉しても
      **無関係な店へ飛ばない**（今回修正した潜在バグ）

### #1136 レビュー投稿中のローディング

- [ ] **投稿中にスピナーが出て、ボタンが押せない**
- [ ] 実際の投稿（数十秒かかるもの）で体感が自然か
- [ ] **失敗したときにローディングが解除され、固まらない**
- [ ] ローディング中に戻る・画像変更などが壊れていない

### #1121 外部リンクを Web で別タブ起動

- [ ] **`/ja-JP/search/topics` の「条件に合うお店を Google Map 上で確認できます」で別タブが開く**
- [ ] **戻ってきてもエラーにならない**（Issue の主症状）
- [ ] `/ja-JP/search/result` の「Google マップで開く」が従来どおり動く
- [ ] 設定画面のレビュー投稿導線（ネイティブ）でストアが開く
- [ ] アプリへ戻す導線（`OpenInAppBanner`）が壊れていない
- [ ] ポップアップブロッカーを有効にした状態での挙動（ブロック時はフィードバックが無い仕様。許容できるか）

### #1129 最近使った場所を押下したら先頭へ

- [ ] **「渋谷駅前おおしま皮膚科 / 渋谷駅」の状態で「渋谷駅」を選ぶと、次回は「渋谷駅 / 渋谷駅前おおしま皮膚科」の順になる**
- [ ] 件数が増えない（重複しない）
- [ ] 上限 5 件が維持される

### #1133 保存料理からの地点検索をリッチに

**設計時から未確認として残っている 2 点**があります。どちらも実機でしか確認できません。

- [ ] **iOS の位置情報許可ダイアログが `BlurModal`（Portal）の上に正しく出る**
  - OS ダイアログなので理屈上は問題ないはずですが、**プロフィールタブから初回許可が発火する経路は今回が初**です
- [ ] **モーダル内で「最近使った場所」5 件＋キーボード表示時にレイアウトが収まる**
  - `Card` が `overflow: hidden` で、最近使った場所パネルは Card の高さを伸ばすため**画面外へはみ出す恐れ**があります
- [ ] 現在地ボタンが押せて、地点が入る
- [ ] 最近使った場所がホームと同じリストとして共有されている
- [ ] ar-SA（RTL）で現在地ボタンの位置が破綻していない

**既知の制約（仕様として残しています）**: 同じ料理カテゴリ×同じ地点で 2 回目に検索したときは、
`entriesKey` が既にキャッシュされているため「最近使った場所」への登録と先頭移動が起きません。

### #1132 ブロック済み料理カテゴリへの用語統一

- [ ] ja-JP で「ブロック済み料理カテゴリ」と表示される
- [ ] 他 7 言語で表示崩れ・訳の不自然さが無い（特に ar-SA の RTL）

---

## 2-B. 第2バッチ（#767 #528 #513 #514 #721 #276 #281）で追加された確認項目

### #528 モーダル内のタップ競合（優先度：高・**挙動が変わります**）

**なぜ自動テストで守れないか**: 実タッチのタイミング競合は実機でしか再現できません。
テストは「親が子のタップを奪う responder を持たない」という**構造**を固定しているだけです。
（削除した `onStartShouldSetResponder` を戻すと 2 テストが赤くなることは実測済みです）

**挙動変更**: 修正前は「モーダル内のどこを触ってもキーボードが閉じる」でしたが、
今は**背景タップと候補押下でのみ閉じます**。`BlurModal` を使う全モーダルに効きます。

- [ ] **初回起動で、オートコンプリート候補をタップして選択できる**（Issue の完了条件そのもの）
- [ ] 背景タップでキーボードが閉じる
- [ ] **モーダル内の余白タップで閉じなくなったことが不自然でないか**（体感の判断）
- [ ] 料理カテゴリ検索でも候補が選べる
- [ ] 「最近使った場所」も選べる
- [ ] iOS / Android 両方

### #1176 地点オートコンプリートの重複除去を撤回（優先度：中）

`#952` / `#1123` の「同名なら同一地点」という畳み込みを撤回し、完全重複だけを畳むようにしました。

- [ ] **`大久保駅` で東京と明石の両方が選べる**（#1176 の完了条件。ユニットテストでは固定済み）
- [ ] `渋谷駅` が 2 件表示になっているが許容できるか（**意図的な差し戻し**です）
- [ ] 同名チェーン店舗が消えていない

**development での API 検証は実施していません。** 変更が「フィルタの削除」であり、
Google のレスポンスをそのまま通す方向なので、実データの type 構成に依存しないためです
（#1123 の失敗は「実データの type を誤って仮定した」ことが原因でした）。

### #276 API の robots.txt（実機確認不要）

デプロイ後に **Cloud Run のログから `/robots.txt` の 404 が消えること**だけ確認してください。

---

## 3. 実機確認が不要なもの

| Issue | 理由 |
| --- | --- |
| #1112 CI 新設 | この PR 自身で workflow が実走し success を確認済み |
| #1131 ログアウト E2E | Playwright で実走・緑を確認済み。#1124 の修正を戻すと赤くなることも実測済み |

---

## 4. 実機とは別に残っている検証

### #1123 の development での API 検証 → **実施済み・合格**（ログ確認のみ未確認）

#1123 は API 変更のため、`.claude/skills/parallel-development/API_TESTING.md` は
「PR commit を development へ一時デプロイして検証し、必ず main へ復旧する」ことを求めています。

**実施しました。そして 1 回目は不合格でした。** この検証が #1123 の再発を検知しています。

#### 経緯: ユニットテストが実装の穴を隠していた

1 回目の検証（対象 SHA `02a697f3`）で `渋谷駅` が **2 件返り不合格**。原因は fixture が実 API と食い違っていたことでした。

| | 関連度順の先頭候補 `ChIJz8MVLFiLGGARXP0DqqhoDow` の types |
| --- | --- |
| fixture（誤） | `train_station`, `subway_station`, `transit_station`, `point_of_interest`, `establishment` |
| **実 API（実測）** | `point_of_interest`, `transportation_service`, `establishment`, **`transit_station`** |

実データでは「駅施設側」の候補が鉄道駅固有 type を持たず、交通系は汎用の `transit_station` だけでした。
PR #1149 のレビューで `transit_station` を `RAIL_STATION_TYPES` から外した結果 **#1123 が再発**していましたが、
fixture が誤っていたためテストは緑のままでした。**fixture を実データへ直すと現実装が赤くなることを実測**しています。

PR #1175 で「畳み込みの根拠（`RAIL_STATION_TYPES`）」と「畳み込みの対象（`COLLAPSIBLE_TRANSIT_TYPES`）」を分離して修正しました。

#### 2 回目の検証結果（対象 SHA `fccb964a`）: 合格

| # | 確認項目 | 判定 | 実測 |
| --- | --- | --- | --- |
| 1 | `渋谷駅` で同名の駅候補が 1 件 | ✅ 合格 | 5 件 → **4 件**。先頭 `ChIJz8MV…` が残り `ChIJnxAA…` が消えた |
| 2 | `新宿駅` / `東京駅` も 1 件 | ✅ 合格 | 同じ構造で解消 |
| 3 | `スターバックス` が複数返る | ✅ 合格 | 5 件全残 |
| 4 | `渋谷駅前` で別地点が消えない | ✅ 合格 | 前回と完全同一の 5 件 |
| 5 | 同名の別バス停が消えない | ✅ 合格 | `市役所前 バス停` で **mainText 完全一致の別バス停（久留米市 / 柳川市）が両方残存** |
| 6 | `nanitabeyo_logs_dev` のエラー | ⚠️ **未確認** | 下記参照 |

条件 5 は 1 回目では該当クエリが見つからず未検証でしたが、2 回目で直接の証拠が取れました。
両候補とも `transit_station` を持ちながら残っており、「同名内に鉄道駅固有 type が無ければ畳み込みの根拠が成立しない」
という設計が実環境で意図どおり効いていることを示します。これは同時に、ルール 3（同名 establishment 同士は全て残す）の実環境確認にもなっています。

- deploy run: [31029432032](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31029432032)（`fccb964a`）
- 復旧 run: [31031259256](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31031259256)（`0ccc730e` = main HEAD、success）

#### 残っている未確認事項

- [ ] **`nanitabeyo_logs_dev` のエラー確認**。ワーカーの runner に GCP 資格情報が付かず（`gcloud auth list` が
      `No credentialed accounts`、`GOOGLE_APPLICATION_CREDENTIALS` が空）、BigQuery MCP もリーダー側で認証切れのため、
      `bq` / MCP のどちらでも実行できませんでした。**「できたことにしない」ため未確認のまま残しています。**
      実施するには `claude-worker.yml` へ `google-github-actions/auth` 相当のステップを足すか、
      BigQuery MCP を再認可した対話セッションから実行してください。
- **`API_COMMIT_ID` の直接照合は原理的に不可**でした。API に `API_COMMIT_ID` を返すエンドポイントもレスポンスヘッダも無く、
      Cloud Run の env を読むには gcloud 認証が要ります。したがって復旧確認は
      「対象 SHA の deploy run が success」かつ「それ以降 development への api-deploy が無い」という**間接的な事実**に依っています。

### #1112 を required check にするか → **required 化を推奨（オーナーの操作が必要）**

`pull_request` トリガーの `pr-check.yml` を新設しましたが、**この PR の時点では required check ではありません**。
branch ruleset は変更していないため、赤くてもマージできます。

当初は「数日運用してから」と判断しましたが、独立したセカンドオピニオン（Fable 5）の指摘を受けて
**この PR のマージ直後に required 化する**へ改めました。判断を変えた根拠は、次の実測です。

- `GET /repos/{owner}/{repo}/rules/branches/main` → `[]`、`GET /branches/main` → `protected: false`。
  **main には現在ゲートが 1 つもありません。** つまり required 化は「ゲートの強化」ではなく「最初のゲートの設置」です。
  当初の「様子見」判断は、既に何らかの防御がある前提に立っていた点で誤りでした。
- `pr-check.yml` の実行履歴は 4 run（success 3 / failure 1）。**flake 0 件、真陽性 1 件**
  （#1130 × #1133 の組み合わせで起きた実在の回帰。#1169 で修正）。標本は小さいものの内訳は良好です。
- 誤判断のコストが非対称です。偽赤のコストは re-run 1 回、見逃しのコストは main の破壊で、
  次の防衛線は nightly の `e2e-web-test.yml` までありません。しかも ruleset を消せば戻せる**可逆な決定**です。
- required 化の典型的な罠（paths フィルタ付き check がステータスを報告せず PR が永久にブロックされる）は
  **`pr-check.yml` には当てはまりません**。意図的にフィルタを付けていないため、全 PR で必ずステータスが付きます。

**この作業では ruleset を設定できませんでした**（セッションのトークンは `admin: false`）。
オーナーが Settings → Rules → Rulesets で次を設定してください。

1. Target branch: `main`
2. Require status checks to pass → `PR Check` を追加
3. `app-expo-check.yml` は **required にしないこと**。paths フィルタがあるため上記の罠を踏みます

**設定時の注意**: PR #1171（SDK54）は現時点で `PR Check` のステータスが付いていません。
`pr-check.yml` が main にも #1171 の head にも無いためで、required 化後は #1171 へ 1 度 push するか
close/reopen して `pull_request` イベントを再発火させる必要があります。1 回の手間でありブロッカーではありません
（#1171 は自前の `app-expo-check.yml` で typecheck・test を既に pass 済みで、実質同じ検査に合格しています）。

`app-expo-check.yml` の static-check ジョブと `pr-check.yml` は typecheck + test が重複します。
実害は二重実行のコスト程度なので、#1171 マージ後に static-check を外すか paths を調整する follow-up で解消してください。
**required 化のブロッカーにはしないでください。**

今後 flake（`pnpm install` のネットワーク断など）が観測されたら、まず re-run で凌ぎ、
頻発するようなら required を外して原因を潰してから戻してください。可逆なので撤退基準を重くする必要はありません。

### 第2バッチで「オーナーの操作が必要」として残ったもの

自動化できず、権限の都合でこのセッションから実行できなかったものです。

| Issue | 残っていること | なぜここで完了できないか |
| --- | --- | --- |
| #767 | `infra/gcp/setup_artifact_registry_cleanup_policy.sh` を `dryrun` → `apply` の順で実行 | runner に GCP 資格情報が無く gcloud を実行できない |
| #767 | **`KEEP_COUNT=10` の妥当性を判断**（11 世代より前へロールバックできなくなる） | 運用方針の判断であり、こちらでは決められない |
| #513 | SQL の修正と実行 | **本番 DB への破壊的操作。スキルが禁止している。** レビューで Critical 5 件（下記） |
| #514 | スプレッドシートの内容共有、本番ログの確認 | 本番環境・スプレッドシートへアクセスできない |
| #281 | **Search Console のプロパティを `app.nanitabeyo.net` へ切り替える** | Search Console にアクセスできない |
| #281 | sitemap の送信（**#721 のプリレンダ修正が入ってから**） | 同上 |
| #721 | (a) 共有プレビューの方針決定（SSR / 事前レンダ / 専用 URL） | アーキテクチャの判断が要る |

#### #513 は実行前に必ずレビューを読んでください

Issue #513 のレビューコメントの結論は「**現状の SQL はそのまま実行するとフェーズ2 が確実にエラーで落ちる**」です。

- `reactions` に `UNIQUE (user_id, target_type, target_id, action_type)` が実在する
  （`infra/supabase/migrations/20250803T0101_create_reactions.sql:14`）
- 重複レコードにリアクションが分散している状況そのものが対象なので、付け替え時にほぼ確実に衝突する
- 仮にエラーを回避しても**いいね等が静かに失われる** Critical が別に 3 件ある

#### #281 と #721 のマージ順に依存関係があります

`generateStaticParams` が無いため、現在は `/ja-JP/...` も `/en-US/...` も**同じ ja-JP 固定の HTML** を返します。
この状態で sitemap（8 言語 40 URL）を送信すると、Google には全部が重複コンテンツに見えます。
**#721 のプリレンダ修正が入ってから送信してください。**

### ワーカーへの `workflows: write` 付与 → **付与しない**

Claude Worker は `.github/workflows/` 配下へ push できません（GitHub App の権限不足でサーバ側が push 全体を拒否）。
今回は「ワーカーが patch を出力し、リーダーが適用して commit」で回避しました。
詳細は `.claude/skills/parallel-development/CORE.md` の該当節を参照してください。

リーダーとセカンドオピニオン（Fable 5）が独立に同じ結論に達したため、**付与しない**で確定します。

- **頻度が低く、回避策が安い。** 13 チケット中 workflow を触ったのは #1112 の 1 件のみ。手順は文書化済みで実際に機能しました。
- **付与すると、上の required check の信頼性が自己崩壊します。** `pull_request` の workflow は
  PR の merge commit 上の定義で実行されるため、`workflows: write` を持つワーカーは
  **自分の PR ブランチで `pr-check.yml` を弱め、その弱めた版で green を取れて**しまいます。
  「ワーカーは workflow を書けない」という現状の制約が、required check の信頼性を支えています。2 つの判断はセットです。
- **prompt injection の到達点が広がります。** ワーカーは Issue / PR 本文という外部入力を読みます。
  権限を与えると、injection の影響が「変なコードを commit する」から
  「`claude-worker.yml` 自身の権限拡大や secrets の取り回し変更」まで広がります。
  GitHub App の権限は path 単位に絞れないため、`.github/workflows/**` 全体への write が all-or-nothing になります。
- **#1112 で 2 回空振りした実害の本体は「権限が無いこと」ではなく「拒否が無音だったこと」**で、
  こちらは `CORE.md` への追記（remote rejected の兆候、実装 prompt への禁止事項明記、`git ls-remote` での branch 実在確認）で緩和済みです。

再検討するのは workflow 変更タスクが恒常化した場合のみです。その場合でも、付与の**前に**
`.github/workflows/**` への変更へ人間レビューを必須化する ruleset（または CODEOWNERS + required review）を入れてください。
