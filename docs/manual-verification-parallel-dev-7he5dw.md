# 実機確認チェックリスト（統合ブランチ `claude/parallel-dev-orchestration-7he5dw`）

このドキュメントは、統合ブランチ `claude/parallel-dev-orchestration-7he5dw` へマージした 13 件の変更のうち、
**自動テストでは守れず、人間が実機・実ブラウザで触らないと確認できない項目**をまとめたものです。

## なぜこれが要るのか

この統合ブランチの検証範囲は次のとおりです。

- jest（ユニット・コンポーネントテスト）
- `tsc --noEmit`
- **Playwright（`e2e-web`）と Detox（`e2e-mobile`）** — 当初はログアウト E2E だけでしたが、
  実機確認へ回していた項目を順次 E2E へ移しました

### 実機確認は最終手段です

実機確認には 2 つの問題があります。

1. **回帰テストにならない。** 人間が 1 回見て通っても、次の変更で壊れたことは誰も気付きません
2. **人間の時間を使う。** 1 往復ごとにコストが発生し、往復回数を事前に見積もれません

そのため「これは実機でしか確認できない」と判断する前に、**必ず E2E で書けないかを検討**しました。
その結果、**当初 11 件あった実機確認項目は 3 件まで減りました。**

残した 3 件は、いずれも**自動化が原理的に不可能**と説明できるものだけです
（実 IdP との往復 / OS のネイティブダイアログ / 触り心地の主観評価）。

各項目には**何が観測できたらクリアか**と、**なぜ E2E で書けないのか**を書いてあります。

---

## 1. 実機でしか確認できないもの（**これだけ**）

**当初は 11 件を実機確認としてお願いしていましたが、E2E 化して 3 件まで絞りました。**
残したのは「自動化が原理的に不可能」と説明できるものだけです。

### 2026-08-07 オーナー確認結果

| 項目 | 判定 | 備考 |
| --- | --- | --- |
| #1135 Web の Google ログインが 1 回目で入る | **✅ OK** | 以下のチェックリストは closed |
| #1133 保存料理からの地点検索 | **✅ OK** | E2E でも固定済み（`saved-topic-location-search.spec.ts`）。**iOS の位置情報許可ダイアログの表示位置は未確認のまま**（下記に残しています） |
| #1126 普通の横スクロールで縦並び UI へ切り替わらない | **✅ OK** | |
| #1126 遅い横スワイプでも一気に飛ぶ | **❌ NG → 修正済み** | `snapMode="page"` へ変更。**再確認をお願いします** |
| #528 モーダル内の余白タップでキーボードが閉じない | **△ 現状維持** | 「ちょっと不自然だが解決策が無いなら諦める」との判断 |

### 2026-08-08 オーナー確認結果と、それに対する修正

| 項目 | 判定 | 対応 |
| --- | --- | --- |
| 投票 URL を LINE へ貼るとタイトルが出ない / 画像が別物 | **❌ NG → 修正済み** | ロケール別の静的 OGP を返す rewrite。**実ホスティングで 8 ロケール検証済み** |
| `/s/:token` の中間画面が不要 | **✅ 対応済み** | 本文末尾で `window.location.replace` して自動遷移する |
| 一括共有の文言が一括に見えない | **✅ 対応済み** | 件数が 2 件以上のときは店名を出さず件数を出す文言へ（8 ロケール） |
| Android + Instagram で「アプリで開く」から出られない | **❌ NG → 修正済み** | `intent://` へ切り替え。**再確認をお願いします**（§1-C） |
| LINE から開くと `No sheet found with tag` が出て search へ戻る | **❌ NG → 修正済み** | `presentSheetSafely` / `dismissSheetSafely`。**再確認をお願いします**（§1-C） |

### #1135 Web で 1 回目の Google ログインで入れるか

**なぜ E2E で書けないか**: 実 Google IdP との往復は bot 検知・利用規約の制約で自動化できません。
競合の解消自体はユニットテストで固定済み（修正前のコードで赤くなることを 2 回の独立レビューが実測）です。

> **✅ 2026-08-07 実機確認で OK。この項目は closed です。** 以下は再発時の切り分け用に残しています。

- [x] **Web で、1 回目の Google ログインでログイン状態になる**（これが完了条件そのもの）
- [x] ログアウト → 再ログインも 1 回目で入る
- [ ] Android / iOS で回帰していない

**devtools での切り分け手順**（再現した場合に原因を絞るため）:

1. Application → Local Storage の `sb-<project-ref>-auth-token` を開き、`user.is_anonymous` を控える（JWT 本体は貼らないこと）
2. Console / Network の **Preserve log** を ON にしてログイン
3. コールバック着地の直後、画面遷移する前に確認する

| 見るもの | 期待 | 外れたときの意味 |
| --- | --- | --- |
| `sb-<ref>-auth-token-code-verifier`（飛ぶ直前） | 存在する | 無ければセッション削除で verifier ごと消えている |
| 同上（着地の数秒後） | 消えている | 交換が実行された証拠 |
| `sb-<ref>-auth-token` の `user.is_anonymous`（着地後） | `false` | `false` なのに画面がゲスト表示なら React state だけ巻き戻っている |
| Network の `POST /auth/v1/token?grant_type=pkce` | 200 | 400 なら `code_verifier` 側の問題 |
| Network の `GET /auth/v1/user` | **出ないはず** | 出ていたら削除したはずの `setSession` が残っている |
| Network の `POST /auth/v1/signup` | **出ないはず** | 出ていたら匿名サインインが OAuth セッションを潰している |

### #1133 iOS の位置情報許可ダイアログ

**なぜ E2E で書けないか**: OS のネイティブダイアログは Detox / Playwright の操作対象外です。

- [ ] **iOS の位置情報許可ダイアログが `BlurModal`（Portal）の上に正しく出る**
  - OS ダイアログなので理屈上は問題ないはずですが、**プロフィールタブから初回許可が発火する経路は今回が初**です

地点が入ること・現在地ボタンが押せることは E2E で固定済みなので、**ダイアログの表示位置だけ**見てください。

### #1126 横スクロールの触り心地（Android）

**なぜ E2E で書けないか**: 「触り心地」は主観評価です。
**代表ケース（横スワイプで縦展開しない／縦スワイプでは展開する）は Detox の `swipe()` で固定済み**なので、
残るのは数値では判断できない部分だけです。

設定値: カルーセル活性化 `activeOffsetX = ±10dp` / 縦とみなして fail `failOffsetY = ±20dp`。
カルーセル側の Pan がシートへ譲る（fail する）のは約 63° 以上の急角度のみです。

#### 2026-08-07 の実機フィードバックと、それを受けた修正

オーナーの実機確認で次の判定をいただきました。

| 観測項目 | 判定 |
| --- | --- |
| 普通に横スクロールしたとき、縦並び UI へ切り替わらない | **OK** |
| 遅い横スワイプでも一気に横へ飛んでしまう（1 件ずつ送りたい） | **NG → 修正しました** |

**後者は #1156（Expo SDK 53→54）の carousel v4→v5 移行で入った回帰でした。**
v4 の `pagingEnabled` は既定 `true`（＝1 スワイプ 1 枚）でしたが、移行先の
`snapMode="nearest"` は **慣性が減衰し切った位置の最寄り**へ吸着するため、
ゆっくりしたスワイプでも数枚まとめて飛びます。`snapMode="page"` へ変更して
v4 の挙動へ戻しました（`"none"` は吸着自体が消えて半端な位置で止まるので不可）。

- [ ] **1 スワイプで 1 件だけ送られる**（← `snapMode="page"` の修正後に再確認をお願いします）

#### 残りの観測項目

- [ ] 斜め方向のスワイプが不自然に感じない
- [ ] **水平から 40°〜60° くらいの「横のつもりだが縦にぶれた」スワイプで、シートが縦に広がらない**
- [ ] 速い横スワイプで違和感が無い

#### ⚠️ ここは「直っている前提」で見ないでください（残っている競合窓）

Codex レビューの指摘（[PR コメント](https://github.com/Ayato-kosaka/nanitabeyo/pull/1174#discussion_r3732085875)）で、
**シート側を止めるタイミングが遅い**ことが確認されています。JS からは閉じ切れないため、
今回は直さずに残しています。実機で許容できるかの判断をお願いしたい、というのがこの項目です。

| | 値 | 意味 |
| --- | --- | --- |
| Android のシートが掴む | 縦へ **8dp** | `BottomSheetBehavior` の touchSlop。横の移動量とは比較しない |
| カルーセルが `draggable=false` にする | 横へ **10dp**（`onScrollStart`） | Pan が活性化して初めて発火する |

つまり **「横へ 10dp 動くより先に、縦へ 8dp ぶれた入力」ではシートが先に掴みます**
（水平から約 38.7° より急な入力）。加えて `draggable` は React の prop なので、
ネイティブへ反映されるまで 1 フレーム前後の遅れがあり、非常に速いスワイプではその窓も残ります。

**「タッチダウン時点で止める」方向の修正は見送りました。** その場合、意図的な縦ドラッグでも
カルーセルの Pan が fail するまで（縦 20dp）シートを掴めず、展開操作に遅れが出ます。
どちらの体感がましかは実機でしか判断できないため、**現状のまま実機で確かめていただき、
気になる場合はその方向へ倒す**という段取りにしています。

- [ ] **意図的に縦へ引いたとき、引っかかりなく展開できる**（← 上の変更を入れると、ここが悪化します）

---

## 1-C. ディープリンク（2026-08-08 の実機フィードバック分）

**アプリ内ブラウザ（IAB）の挙動は自動化できません。** UA を偽装しても、
「WebView が https のナビゲーションを内部で処理してしまい App Links が発火しない」というのは
実際の Instagram / LINE の実装依存の挙動で、実機でしか観測できないためです。

どの UA でどの URL を返すかは `app-expo/lib/openInAppUrl.test.ts` で全分岐を固定済みです。
残るのは「その URL を踏んだとき OS が実際にアプリを起こすか」の一点だけです。

### Android + Instagram（今回直したもの）

https の URL は WebView 内で完結してしまうため、Android 専用の `intent://` URI へ切り替えました。

- [ ] Instagram のアプリ内ブラウザで投票 URL を開き、**「アプリで開く」でアプリが起動する**
- [ ] アプリ未インストールの端末では **Play ストアへ飛ぶ**（`S.browser_fallback_url`）

### 巻き添えになっていないこと（← ここが重要）

`intent://` を返すのは **Android かつ Meta 系 IAB のときだけ**です。動いている経路を壊していないことの確認です。

- [ ] **Android + LINE** から従来どおりアプリが開く
- [ ] **通常の Android Chrome** から従来どおりアプリが開く
- [ ] **iOS**（Safari / Instagram）が従来どおり

### LINE から起動したときのシートのエラー

`No sheet found with tag …` が出たあと search 画面へ戻される、という症状への対応です。
ディープリンク起動直後は「search タブがいったんマウント → チュートリアルシートが present を予約 →
すぐ投票画面へ遷移してネイティブビューが破棄される」という順序になり、
破棄済みの tag への present が **unhandled promise rejection** になっていました。

- [ ] LINE の共有リンクからアプリを起動したとき、**投票画面に着地する**（search へ戻されない）
- [ ] コンソールに `No sheet found with tag` が出ない

---

## 1-D. 実機確認セッション用まとめ（2026-08-08）

### 環境（この状態で確認してください）

| 対象 | 状態 |
| --- | --- |
| `https://nanitabeyo-dev.web.app` | ブランチ `0c674259`（[run 31327184296](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31327184296)） |
| `api-development` | **ブランチ `648d7ede`**（[run 31311543420](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31311543420)）。オーナー指示により main へ戻していません |
| アプリ | **preview チャンネルへ OTA 配信済み**（`382ab164`）。アプリを一度終了して再起動してください |

⚠️ **どこを直したかで、反映に必要な操作が違います。**

| 修正 | 反映先 | 必要な操作 |
| --- | --- | --- |
| 投票 URL の OGP | Web（Hosting） | 無し（デプロイ済み） |
| Android の `intent://`（`OpenInAppBanner`） | **Web のみ**（このコンポーネントはネイティブでは何も描画しない） | 無し（デプロイ済み） |
| `/s/:token` の自動遷移・一括共有の文言 | API（Cloud Run） | 無し（デプロイ済み） |
| TrueSheet / `useLocale` / ログのバッチ分割 | **ネイティブアプリ** | **preview の OTA 適用**（アプリを終了 → 再起動） |

⚠️ `API_TESTING.md` の規約では、検証後に api-development を main へ復旧する必要があります。
実機確認が終わったら声をかけてください（こちらで復旧まで実施します）。

### 2026-08-09 オーナー確認結果と、それに対する修正

| 項目 | 判定 | 対応 |
| --- | --- | --- |
| C. Android + Instagram の「アプリで開く」 | **✅ OK** | `intent://` で解決 |
| G. #1133 iOS の位置情報許可ダイアログ | **✅ OK** | クローズ可能 |
| A. 投票カードの文言がアプリ紹介文で「友達投票」だと分からない | **❌ NG → 修正済み** | 投票専用の静的 SEO（8 ロケール）を prerender に載せ、rewrite の宛先を投票ページへ変更 |
| B / E / G#1126 | **未確認**（アプリ側の不具合で到達できず） | preview チャンネルへ OTA 配信済み（下記） |

#### アプリの OTA 配信（B / E / G#1126 の前提）

| | |
| --- | --- |
| チャンネル | `preview` |
| runtime version | `1.13`（ブランチで変えていないので既存の preview ビルドに当たります） |
| プラットフォーム | iOS / Android 両方 |
| commit | `382ab164`（[run 31326842465](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31326842465)） |

アプリを一度終了して再起動すると取り込まれます（起動時にチェック → 次回起動で適用、が Expo Updates の既定）。

⚠️ **preview チャンネルが向いている API は未確認です。** `/s/` の共有（B）は
`POST /v1/share-links` を持つ **api-development** に向いている必要があります。
共有したときに `/s/...` ではなく `?ids=...` の URL になる場合は preview が
api-production を向いているサインなので、そう伝えてください（こちらで向き先を調整します）。

### A. 投票 URL の OGP（文言を直しました）

`https://nanitabeyo-dev.web.app/ja-JP/search/dish-category-group-votes/<token>/vote`

**2026-08-09 の指摘**: OGP は出るようになったが、タイトルがアプリ紹介文のままで
「友達投票への招待」だと分からない。

**原因**: rewrite の宛先が «検索画面» の prerender だった。
かつ、画面側に `useSeo` を足しても直りません — `useSeo` は `useFocusEffect` の中で push するため
**expo export の prerender では一切走らない**ためです。
`[locale]/_layout.tsx` の `seoDefaults`（レンダー時に評価される useMemo）をパス依存にして、
prerender 済み HTML に投票専用の文言が載るようにし、rewrite の宛先も投票ページへ変えました。

実ホスティングで検証済み（[run 31327184296](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31327184296) / 9 passed）。
ja-JP は「友達と投票して、今日食べる料理を決めよう」、en-US は "Vote with friends and decide what to eat" になります。

- [ ] LINE に貼ると **投票への招待だと分かるタイトル**が出る
- [ ] `/en-US/...` の URL では英語のタイトルになる

⚠️ og:image はロケール既定のブランド画像のままです（投票専用の画像は作っていません）。
画像も投票用にしたい場合は `app-expo/public/og/` へ 8 枚（1200x630）追加する作業が別途要ります。

⚠️ **以前貼った URL で試さないでください。** LINE / Facebook は OGP を強くキャッシュするので、
直っていても古いカードが出ます。**新しい token で新規に貼る**か、各社のデバッガでキャッシュを飛ばしてください。

### B. `/s/:token` 共有リンク（api-development がブランチになったので今日から通ります）

- [ ] 共有ボタンで `/s/...` の URL が発行される（`?ids=` に戻らない）
- [ ] **未ログイン（匿名）状態でも共有できる** ← `share_links_created_by_fkey` の回帰確認。ここが最重要
- [ ] `/s/...` を開くと **中間画面が出ずに** 目的の画面へ自動遷移する
- [ ] 複数件まとめて共有したとき、文言が「一括共有」に見える（店名ではなく件数が出る）
- [ ] アプリを入れた端末で `/s/...` を開くと、ホームではなく **目的の画面** に着地する

### C. Android の「アプリで開く」

- [ ] **Instagram のアプリ内ブラウザ**で投票 URL を開き、「アプリで開く」でアプリが起動する
- [ ] アプリ未インストールの端末では Play ストアへ飛ぶ

巻き添えになっていないこと（`intent://` は Meta 系 IAB のときだけ返します）:

- [ ] **Android + LINE** から従来どおり開く
- [ ] **通常の Android Chrome** から従来どおり開く
- [ ] **iOS**（Safari / Instagram）が従来どおり

### D. LINE から起動したときのシート

- [ ] LINE の共有リンクからアプリを起動して、**投票画面に着地する**（search へ戻されない）
- [ ] `No sheet found with tag …` が出ない

### E. 起動直後の検索（今回直した 400）

- [ ] アプリを**完全終了 → 起動**して、最初の検索が **1 回で成功する**
- [ ] コンソールに `languageTag must follow IETF BCP 47 format` の 400 が出ない

### F. フロントログ

- [ ] しばらく触っても `⚠️ frontend log batch dropped: kind=transient status=500` が出ない

### G. 前回からの持ち越し

- [ ] #1126 1 スワイプで 1 件だけ送られる（`snapMode="page"`）
- [ ] #1126 水平から 40〜60° の「横のつもりだが縦にぶれた」スワイプでシートが広がらない
- [ ] #1133 iOS の位置情報許可ダイアログが `BlurModal` の上に出る

### 1-D-1. 起動直後の検索が 400 になっていた件（原因と修正）

実機のフロントログに証拠がありました。

```
requestPayload: { address: "…", languageTag: "", localLanguageCode: "ja", … }
status: 400  languageTag must follow IETF BCP 47 format …, languageTag must be a string
```

`languageTag` が **空文字**で、`fetchWithAuth` の `toQueryString` が空文字を «未指定» として落とすため、
サーバからはパラメータ自体が無いように見えて 400 になっていました。
**0.8 秒後の再実行は成功**しているので、起動直後の自動検索だけが失敗していたことになります
（ユーザーには一瞬のエラー、こちらには Google Places クォータの無駄消費。実際にこの日クォータ超過の 429 も出ています）。

原因は `useLocale` が `pathname.split("/")[1]` をそのまま返していたことです。
`usePathname()` は**起動直後やリダイレクトの途中で `"/"` を返します**。

**同じ前提崩れがもう 1 つありました。** `/s/:token` では先頭セグメントが `s` なので
**ロケールが `"s"` になり**、400 にはならないまま `languageTag=s` が静かにサーバへ飛んでいました。

呼び出し側は 38 箇所あるため、`useLocale` で必ず妥当な値へ寄せる形にしています。

⚠️ 判定は `PUBLIC_LOCALES` の完全一致ではなく **BCP 47 の «形»** で行っています。
完全一致にすると `/pt-BR/...` のような未対応ロケール URL の挙動まで変わってしまうためです。
退避先も `ja-JP` 決め打ちではなく端末の言語から解決します（英語端末の起動直後だけ日本語で検索する、を避けるため）。

---

## 1-B. E2E へ移して実機確認が不要になったもの

**以前このドキュメントで実機確認をお願いしていた項目のうち、次は E2E で守られるようになりました。**
実機確認は回帰テストにならないため、自動化できるものはすべて自動化しています。

| Issue | 内容 | web (Playwright) | mobile (Detox) |
| --- | --- | --- | --- |
| #1120 | ゲスト用 UI のちらつき | ✅ | ✅（検出力に差あり※） |
| #1121 | 外部リンクの別タブ起動 | ✅ | —（web 固有） |
| #1122 | モーダルを閉じてから遷移 | ✅ | ✅ |
| #1129 | 最近使った場所を先頭へ | ✅ | ✅ |
| #1131 | ログアウト | ✅ | ✅ |
| #1132 | ブロック済み料理カテゴリの文言 | ✅ | ✅ |
| #1136 | 投稿中のローディング | ✅ | ✅ |
| #528 | モーダル内のタップ競合 | — （ネイティブ固有） | ✅ |
| #1130 | SafeArea への食い込み | —（Android 固有） | ✅（実座標を比較実測※※） |
| #1133 | 地点検索（許可ダイアログ以外） | ✅ | ✅ |

※ #1120 の mobile は Detox に `page.route()` 相当が無く detail をモックできないため、
**「最終状態にゲスト用 UI が無い」という弱い不変条件**に絞っています。
ゲスト用を描いてから差し替える実装へ戻った場合、mobile では捕まりません（**web が担保します**）。

※※ #1130 は閾値をハードコードせず、**Issue 本文が見本に挙げた「ブロック済み料理カテゴリ一覧」の
ヘッダー上端 y を同じ端末上で読み、その差**を見ています。safe area inset の実値は端末で変わり、
Android の `frame` は dp ではなく物理ピクセルで返るため、絶対値の閾値はどの端末でも意味を持ちません。
許容差は「タイトル 1 行の高さの半分」（≒12dp 相当）で、意図した差（+8dp）より大きく、
最小のステータスバー高さ（24dp）より小さいので、**修正後は通り、修正前は必ず落ちます。**

**「両方書いた」で検出力の差を隠さない**ため、ここに明記しています。

---

## 2-B. 第2バッチ（#767 #528 #513 #514 #721 #276 #281）で追加された確認項目

### #528 モーダル内のタップ競合 → **Detox へ移しました。ただし挙動変更の体感だけ確認をお願いします**

**当初は実機確認としてお願いしていましたが、これは本来 Detox でこそ捕まえるべき不具合**でした。
「候補をタップしたのにキーボードだけ閉じて無反応」は E2E のタップ操作で再現できます。

E2E で固定済み（実機確認不要）:
- 地点オートコンプリートの候補をタップすると、実際にその地点が選択される
- 料理カテゴリ検索でも同様
- 背景タップでキーボードが閉じる

ユニットテストでも「親が子のタップを奪う responder を持たない」構造を固定しており、
削除した `onStartShouldSetResponder` を戻すと 2 テストが赤くなることを実測済みです。

**残る確認（体感の判断のみ）**:

修正前は「モーダル内のどこを触ってもキーボードが閉じる」でしたが、
今は**背景タップと候補押下でのみ閉じます**。`BlurModal` を使う全モーダルに効く挙動変更です。

- [x] **モーダル内の余白タップで閉じなくなったことが不自然でないか**（これは主観評価なので E2E で判断できません）

**2026-08-07 の実機フィードバック（判定: 現状維持）**

> ちょっと不自然だけど、解決策がないなら諦める。

「余白タップでも閉じる」を戻すには、削除した `onStartShouldSetResponder` を親へ復活させることに
なりますが、それは **#528 の不具合そのもの**（親が子のタップを奪い、候補を押しても無反応）を
再発させます。両立するには「余白か候補か」を親が判定する必要があり、判定条件が増えるほど
`BlurModal` を使う全モーダルで壊れ方が読めなくなります。

**トレードオフを承知のうえで現状維持と判断されました。** 後から「やはり閉じたい」となった場合は、
背景タップ領域を余白まで広げる（候補リストの外側だけに `Pressable` を敷く）方向が、
親の responder を戻すより安全です。

### #1176 地点オートコンプリートの重複除去を撤回（優先度：中）

`#952` / `#1123` の「同名なら同一地点」という畳み込みを撤回し、完全重複だけを畳むようにしました。

- [ ] **`大久保駅` で東京と明石の両方が選べる**（#1176 の完了条件。ユニットテストでは固定済み）
- [ ] `渋谷駅` が 2 件表示になっているが許容できるか（**意図的な差し戻し**です）
- [ ] 同名チェーン店舗が消えていない

**development での API 検証は実施していません。** 変更が「フィルタの削除」であり、
Google のレスポンスをそのまま通す方向なので、実データの type 構成に依存しないためです
（#1123 の失敗は「実データの type を誤って仮定した」ことが原因でした）。

### #276 API の robots.txt（実機確認不要）

デプロイ後に **Cloud Run のログから `/robots.txt` の 404 が消えること**だけ確認してください。

---

## 3. 実機確認が不要なもの

| Issue | 理由 |
| --- | --- |
| #1112 CI 新設 | この PR 自身で workflow が実走し success を確認済み |
| #1131 ログアウト E2E | Playwright で実走・緑を確認済み。#1124 の修正を戻すと赤くなることも実測済み |

---

## 4. 実機とは別に残っている検証

### #1123 の development での API 検証 → **実施済み・合格。ただしその後 #1176 のため方針ごと撤回しました**

> ⚠️ **以下の記録は歴史的な経緯です。** この検証で合格した直後に、この修正が別の重大なバグ
> [#1176](https://github.com/Ayato-kosaka/nanitabeyo/issues/1176)（同名の別駅が畳まれて選べない）を生んでいることが分かり、
> **PR #1177 で対応そのものを撤回しました。** 現在の `dedupeAutocompletePlaces` は
> 「mainText と secondaryText が両方とも完全一致するものだけを重複とみなす」まで後退しています。
> 経緯は [#1123 のクロージングコメント](https://github.com/Ayato-kosaka/nanitabeyo/issues/1123#issuecomment-5204619847) を参照してください。
>
> それでもこの節を残しているのは、**「テストが緑でも実装は壊れている」を development 検証が実際に検知した記録**として
> 価値があるためです。撤回の判断もこの検証があって初めて可能になりました。

#1123 は API 変更のため、`.claude/skills/parallel-development/API_TESTING.md` は
「PR commit を development へ一時デプロイして検証し、必ず main へ復旧する」ことを求めています。

**実施しました。そして 1 回目は不合格でした。** この検証が #1123 の再発を検知しています。

#### 経緯: ユニットテストが実装の穴を隠していた

1 回目の検証（対象 SHA `02a697f3`）で `渋谷駅` が **2 件返り不合格**。原因は fixture が実 API と食い違っていたことでした。

| | 関連度順の先頭候補 `ChIJz8MVLFiLGGARXP0DqqhoDow` の types |
| --- | --- |
| fixture（誤） | `train_station`, `subway_station`, `transit_station`, `point_of_interest`, `establishment` |
| **実 API（実測）** | `point_of_interest`, `transportation_service`, `establishment`, **`transit_station`** |

実データでは「駅施設側」の候補が鉄道駅固有 type を持たず、交通系は汎用の `transit_station` だけでした。
PR #1149 のレビューで `transit_station` を `RAIL_STATION_TYPES` から外した結果 **#1123 が再発**していましたが、
fixture が誤っていたためテストは緑のままでした。**fixture を実データへ直すと現実装が赤くなることを実測**しています。

PR #1175 で「畳み込みの根拠（`RAIL_STATION_TYPES`）」と「畳み込みの対象（`COLLAPSIBLE_TRANSIT_TYPES`）」を分離して修正しました。

#### 2 回目の検証結果（対象 SHA `fccb964a`）: 合格

| # | 確認項目 | 判定 | 実測 |
| --- | --- | --- | --- |
| 1 | `渋谷駅` で同名の駅候補が 1 件 | ✅ 合格 | 5 件 → **4 件**。先頭 `ChIJz8MV…` が残り `ChIJnxAA…` が消えた |
| 2 | `新宿駅` / `東京駅` も 1 件 | ✅ 合格 | 同じ構造で解消 |
| 3 | `スターバックス` が複数返る | ✅ 合格 | 5 件全残 |
| 4 | `渋谷駅前` で別地点が消えない | ✅ 合格 | 前回と完全同一の 5 件 |
| 5 | 同名の別バス停が消えない | ✅ 合格 | `市役所前 バス停` で **mainText 完全一致の別バス停（久留米市 / 柳川市）が両方残存** |
| 6 | `nanitabeyo_logs_dev` のエラー | ⚠️ **未確認** | 下記参照 |

条件 5 は 1 回目では該当クエリが見つからず未検証でしたが、2 回目で直接の証拠が取れました。
両候補とも `transit_station` を持ちながら残っており、「同名内に鉄道駅固有 type が無ければ畳み込みの根拠が成立しない」
という設計が実環境で意図どおり効いていることを示します。これは同時に、ルール 3（同名 establishment 同士は全て残す）の実環境確認にもなっています。

- deploy run: [31029432032](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31029432032)（`fccb964a`）
- 復旧 run: [31031259256](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31031259256)（`0ccc730e` = main HEAD、success）

#### 残っている未確認事項

- [ ] **`nanitabeyo_logs_dev` のエラー確認**。ワーカーの runner に GCP 資格情報が付かず（`gcloud auth list` が
      `No credentialed accounts`、`GOOGLE_APPLICATION_CREDENTIALS` が空）、BigQuery MCP もリーダー側で認証切れのため、
      `bq` / MCP のどちらでも実行できませんでした。**「できたことにしない」ため未確認のまま残しています。**
      実施するには `claude-worker.yml` へ `google-github-actions/auth` 相当のステップを足すか、
      BigQuery MCP を再認可した対話セッションから実行してください。
- **`API_COMMIT_ID` の直接照合は原理的に不可**でした。API に `API_COMMIT_ID` を返すエンドポイントもレスポンスヘッダも無く、
      Cloud Run の env を読むには gcloud 認証が要ります。したがって復旧確認は
      「対象 SHA の deploy run が success」かつ「それ以降 development への api-deploy が無い」という**間接的な事実**に依っています。

### #1112 を required check にするか → **required 化を推奨（オーナーの操作が必要）**

`pull_request` トリガーの `pr-check.yml` を新設しましたが、**この PR の時点では required check ではありません**。
branch ruleset は変更していないため、赤くてもマージできます。

当初は「数日運用してから」と判断しましたが、独立したセカンドオピニオン（Fable 5）の指摘を受けて
**この PR のマージ直後に required 化する**へ改めました。判断を変えた根拠は、次の実測です。

- `GET /repos/{owner}/{repo}/rules/branches/main` → `[]`、`GET /branches/main` → `protected: false`。
  **main には現在ゲートが 1 つもありません。** つまり required 化は「ゲートの強化」ではなく「最初のゲートの設置」です。
  当初の「様子見」判断は、既に何らかの防御がある前提に立っていた点で誤りでした。
- `pr-check.yml` の実行履歴は 4 run（success 3 / failure 1）。**flake 0 件、真陽性 1 件**
  （#1130 × #1133 の組み合わせで起きた実在の回帰。#1169 で修正）。標本は小さいものの内訳は良好です。
- 誤判断のコストが非対称です。偽赤のコストは re-run 1 回、見逃しのコストは main の破壊で、
  次の防衛線は nightly の `e2e-web-test.yml` までありません。しかも ruleset を消せば戻せる**可逆な決定**です。
- required 化の典型的な罠（paths フィルタ付き check がステータスを報告せず PR が永久にブロックされる）は
  **`pr-check.yml` には当てはまりません**。意図的にフィルタを付けていないため、全 PR で必ずステータスが付きます。

**この作業では ruleset を設定できませんでした**（セッションのトークンは `admin: false`）。
オーナーが Settings → Rules → Rulesets で次を設定してください。

1. Target branch: `main`
2. Require status checks to pass → `PR Check` を追加
3. `app-expo-check.yml` は **required にしないこと**。paths フィルタがあるため上記の罠を踏みます

**設定時の注意**: PR #1171（SDK54）は現時点で `PR Check` のステータスが付いていません。
`pr-check.yml` が main にも #1171 の head にも無いためで、required 化後は #1171 へ 1 度 push するか
close/reopen して `pull_request` イベントを再発火させる必要があります。1 回の手間でありブロッカーではありません
（#1171 は自前の `app-expo-check.yml` で typecheck・test を既に pass 済みで、実質同じ検査に合格しています）。

`app-expo-check.yml` の static-check ジョブと `pr-check.yml` は typecheck + test が重複します。
実害は二重実行のコスト程度なので、#1171 マージ後に static-check を外すか paths を調整する follow-up で解消してください。
**required 化のブロッカーにはしないでください。**

今後 flake（`pnpm install` のネットワーク断など）が観測されたら、まず re-run で凌ぎ、
頻発するようなら required を外して原因を潰してから戻してください。可逆なので撤退基準を重くする必要はありません。

### 実環境で検証を済ませたもの（実機確認は不要）

「テストできるところは実環境に上げてテストする」方針で、次は**実測で合格**を確認しました。実機確認の対象外です。

| Issue | 検証環境 | 実測 |
| --- | --- | --- |
| #276 | **development API** | `/robots.txt` が 200 / `text/plain; charset=utf-8` / `public, max-age=86400, immutable` / 本文 26 バイト（hexdump 確認）。`/v1/robots.txt` は 404 |
| #1176 | **development API** | `大久保駅` で東京（新宿区百人町）・兵庫（明石市）・京都（宇治市）が**全て残る**。`渋谷駅` は 2 件（意図的な差し戻し） |
| #721 (b) | **Hosting エミュレータ** | `/en-US/posts` の `<title>` が `CraveCatch - Find your dish`、`/ja-JP/posts` が日本語、`/fr-FR/posts` が `og:locale=fr_FR` |
| #281 | **Hosting エミュレータ** | `/sitemap.xml` が `application/xml` で XML を返す（従来は index.html を 200 で返していた）。`/robots.txt` は `text/plain` |

API 検証は `API_TESTING.md` に従い、**検証後に main（`0ccc730`）を development へ再デプロイして復旧**しています。

Hosting の検証は**ローカルエミュレータのみ**で、実デプロイはしていません。本番の解決順序が一致する前提での判定です。

### 第2バッチで「オーナーの操作が必要」として残ったもの

自動化できず、権限の都合でこのセッションから実行できなかったものです。

| Issue | 残っていること | なぜここで完了できないか |
| --- | --- | --- |
| ~~#767~~ | ~~`setup_artifact_registry_cleanup_policy.sh` を `dryrun` → `apply`~~ | **✅ 2026-08-07 オーナー対応済み**（[#767 コメント](https://github.com/Ayato-kosaka/nanitabeyo/issues/767#issuecomment-5220826415)） |
| ~~#767~~ | ~~`KEEP_COUNT=10` の妥当性を判断~~ | **✅ 2026-08-07 オーナー対応済み**（同上） |
| #513 | — | **今回のバッチから取り下げ**（オーナー判断）。コード変更は入れていません。再開時は SQL の Critical 5 件（下記）を先に潰してください |
| #514 | **② 本番デプロイ → ③ リラン実行**（① 権限付与は対応済み） | **2026-08-07 オーナーが [#514 コメント](https://github.com/Ayato-kosaka/nanitabeyo/issues/514#issuecomment-5221187493) の対応を追加。リランの手順は #1194 に明記済み。** 調査と修正は完了（PR #1183）、手順は `api/src/internal/resize-image/README.md` |
| #281 | main へマージ・本番デプロイ後、**Search Console で sitemap を再送信** | Search Console にアクセスできない。**URL 数が 371 → 32 に減ります（意図した縮小）** |
| ~~#721 (a)~~ | ~~共有プレビューの方針決定~~ | **✅ 設計・実装・実環境検証まで完了。** `/s/:token` の Share Link Resolver として実装し、`https://nanitabeyo-dev.web.app` に対する実 HTTP 検証で **22 件すべて OK**（[run 31212601679](https://github.com/Ayato-kosaka/nanitabeyo/actions/runs/31212601679)）。設計は `docs/share-links.md` |

#### #513 は実行前に必ずレビューを読んでください

Issue #513 のレビューコメントの結論は「**現状の SQL はそのまま実行するとフェーズ2 が確実にエラーで落ちる**」です。

- `reactions` に `UNIQUE (user_id, target_type, target_id, action_type)` が実在する
  （`infra/supabase/migrations/20250803T0101_create_reactions.sql:14`）
- 重複レコードにリアクションが分散している状況そのものが対象なので、付け替え時にほぼ確実に衝突する
- 仮にエラーを回避しても**いいね等が静かに失われる** Critical が別に 3 件ある

#### #281 と #721 (b) のマージ順に依存関係がありました → **解消済み**

`generateStaticParams` が無い間は `/ja-JP/...` も `/en-US/...` も**同じ ja-JP 固定の HTML** を返しており、
その状態で sitemap を送信すると Google には全部が重複コンテンツに見えます。

**#721 (b)（en-US が英語にならない）は PR #1182 で解決済み**で、Hosting エミュレータでの実測も取れています
（`/en-US/posts` の `<title>` が英語、`/fr-FR/posts` の `og:locale` が `fr_FR`）。
したがって **sitemap は送信して構いません**。残っている #721 (a)（共有プレビューの方式）は sitemap とは独立です。

### ワーカーへの `workflows: write` 付与 → **付与しない**

Claude Worker は `.github/workflows/` 配下へ push できません（GitHub App の権限不足でサーバ側が push 全体を拒否）。
今回は「ワーカーが patch を出力し、リーダーが適用して commit」で回避しました。
詳細は `.claude/skills/parallel-development/CORE.md` の該当節を参照してください。

リーダーとセカンドオピニオン（Fable 5）が独立に同じ結論に達したため、**付与しない**で確定します。

- **頻度が低く、回避策が安い。** 13 チケット中 workflow を触ったのは #1112 の 1 件のみ。手順は文書化済みで実際に機能しました。
- **付与すると、上の required check の信頼性が自己崩壊します。** `pull_request` の workflow は
  PR の merge commit 上の定義で実行されるため、`workflows: write` を持つワーカーは
  **自分の PR ブランチで `pr-check.yml` を弱め、その弱めた版で green を取れて**しまいます。
  「ワーカーは workflow を書けない」という現状の制約が、required check の信頼性を支えています。2 つの判断はセットです。
- **prompt injection の到達点が広がります。** ワーカーは Issue / PR 本文という外部入力を読みます。
  権限を与えると、injection の影響が「変なコードを commit する」から
  「`claude-worker.yml` 自身の権限拡大や secrets の取り回し変更」まで広がります。
  GitHub App の権限は path 単位に絞れないため、`.github/workflows/**` 全体への write が all-or-nothing になります。
- **#1112 で 2 回空振りした実害の本体は「権限が無いこと」ではなく「拒否が無音だったこと」**で、
  こちらは `CORE.md` への追記（remote rejected の兆候、実装 prompt への禁止事項明記、`git ls-remote` での branch 実在確認）で緩和済みです。

再検討するのは workflow 変更タスクが恒常化した場合のみです。その場合でも、付与の**前に**
`.github/workflows/**` への変更へ人間レビューを必須化する ruleset（または CODEOWNERS + required review）を入れてください。
